
int many(int x) { 
	x = x * 2 + 4 % 3 / 2; //shouldn't break, and do in order of operands
	return x;
}

int main() {
	int xyz;
	xyz = many(5);
    // prints 5 * 2 + 4 % 3 / 2 = 10
	write xyz;
	return 0;
}
