// making sure that many adds in a row works
int add(int i, int j, int k)
{
    return i + j + k;
}

int addMore(int x, int y)
{
    return x + y;
}

int main()
{
    int a = 1;
    int b = 2;
    int c = 4;
    int d = 8;
    int e = 16;
    // trouble getting this to work
    // 7 + (8 + 16) = 31
    write add(a, b, c) + addMore(d, e);
    return 0;
}